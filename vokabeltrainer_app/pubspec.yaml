name: vokabeltrainer_app
description: Ein einfacher Vokabeltrainer
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  csv: ^5.1.1
  flutter_map: ^6.0.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/images/static_map.png               # Fallback-PNG
    - assets/Stationenbeschreibung-englisch.csv  # Stations-CSV
    - assets/Vokabeln alle.csv                             # (optional) Station-Bilder
    - assets/images/
