name: vokabeltrainer_app
description: Ein einfacher Vokabeltrainer
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: ^4.0.0      # (oder deine vorhandene Version)
  # … weitere Abhängigkeiten

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/images/                         #  ← Bilder hier ablegen (1.jpg, 2.png …)
    - assets/Stationenbeschreibung-englisch.csv
