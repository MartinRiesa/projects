name: vokabeltrainer_app
description: Ein einfacher Vokabeltrainer
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # weitere Pakete hier eintragen, z. B.
  # provider: ^6.1.5
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2   # oder deine bevorzugte Version

flutter:
  uses-material-design: true
  assets:
    - assets/
